---
- name: Ensure app directory exists
  file:
    path: '{{ app_dest }}'
    state: directory
    owner: ubuntu
    group: ubuntu
    mode: '0755'

- name: Stop and remove previous standalone container from earlier deploy
  community.docker.docker_container:
    name: node-hello-service
    state: absent
  ignore_errors: true

- name: Deploy production docker-compose.yml
  template:
    src: docker-compose.prod.yml.j2
    dest: '{{ app_dest }}/docker-compose.yml'
    owner: ubuntu
    group: ubuntu
    mode: '0644'

- name: Deploy .env file for the application
  template:
    src: app.env.j2
    dest: '{{ app_dest }}/.env'
    owner: ubuntu
    group: ubuntu
    mode: '0600'

- name: Pull latest images
  command: docker compose pull
  args:
    chdir: '{{ app_dest }}'

- name: Start application stack
  command: docker compose up -d
  args:
    chdir: '{{ app_dest }}'
