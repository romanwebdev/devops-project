---
- name: Install nginx
  apt:
    name: nginx
    state: present
    update_cache: true

- name: Ensure nginx is enabled and started
  service:
    name: nginx
    state: started
    enabled: true
